#include <stdio.h>
#include <stdbool.h>
#include <string.h>

#define N 20 // max number of letters in the name

typedef struct


typedef struct{
    char Name[N];
    int life_points, attack_points, defense_points;
    //skills
}Character;